{
 "run_at_utc": "2026-09-10T17:19:14+00:00",
 "base": "https://quant-india.onrender.com",
 "passed": [
  "build max_loss=5%: JSON a browser can parse (no bare NaN/Infinity)",
  "build max_loss=5%: meets_loss_limit agrees with its own downside",
  "build max_loss=20%: JSON a browser can parse (no bare NaN/Infinity)",
  "build: returns positions",
  "build: every position is positive",
  "build: positions sum to the amount asked for",
  "build: no single position above 60%",
  "build max_loss=20%: meets_loss_limit agrees with its own downside",
  "build max_loss=60%: JSON a browser can parse (no bare NaN/Infinity)",
  "build max_loss=60%: meets_loss_limit agrees with its own downside",
  "scenarios: JSON a browser can parse (no bare NaN/Infinity)",
  "scenarios: all 4 outcome blocks are internally coherent",
  "scenarios: every delta equals after minus base (3 scenarios)",
  "shock -20% market: JSON a browser can parse (no bare NaN/Infinity)",
  "shock: a -20% market shock is not reported as a gain",
  "suggest-fix 95/5: JSON a browser can parse (no bare NaN/Infinity)",
  "suggest-fix: returns concrete steps for a 95/5 portfolio",
  "suggest-fix: calls a 95/5 portfolio concentrated",
  "suggest-fix: reports both the typical outcome and the downside",
  "suggest-fix: says re-weighting cannot fix a two-stock portfolio",
  "scenarios: never adds a stock the user does not already hold",
  "scenarios: advises more names without picking them",
  "fit INFY: JSON a browser can parse (no bare NaN/Infinity)",
  "fit: a priceable stock is judged on correlation, not only weights",
  "fit SMALL250: JSON a browser can parse (no bare NaN/Infinity)",
  "what-if refuses an empty portfolio",
  "what-if refuses two nonexistent tickers",
  "what-if refuses a security the scan cannot price (SMALL250)",
  "what-if: a nonexistent ticker beside a real one — refused, and the refusal names the holding",
  "what-if: a typo (RELIANC.NS) beside a real one — refused, and the refusal names the holding",
  "what-if: a negative holding is not displayed while being ignored",
  "optimizer stability max_weight=1.0: JSON a browser can parse (no bare NaN/Infinity)",
  "stability: an unconstrained 100% corner is flagged, not praised",
  "stability max_weight=1.0: weights pinned at their bounds are not called stable",
  "optimizer stability max_weight=0.4: JSON a browser can parse (no bare NaN/Infinity)"
 ],
 "failed": [
  "fit: never claims overlap without having measured correlation",
  "what-if: a negative holding is refused or explained",
  "stability max_weight=0.4: weights pinned at their bounds are not called stable"
 ],
 "notes": [
  [
   "build_downside_by_stated_loss_limit",
   {
    "5": -10.09,
    "20": -10.09,
    "60": -10.09
   }
  ],
  [
   "scenarios_how_to_read",
   "Each option shows the change in BOTH typical outcome and worst case. Most improvements are trades — a little expected return given up for a smaller loss when things go wrong."
  ],
  [
   "scenarios_disclaimer",
   "Simulated from past returns. Not a prediction, not financial advice."
  ],
  [
   "scenarios_that_add_stocks_not_held",
   []
  ],
  [
   "what_if_untweaked_95_5_mentions_concentration_outside_weights",
   false
  ]
 ]
}