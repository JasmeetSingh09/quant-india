fastapi
uvicorn[standard]
python-multipart
psycopg2-binary
python-dotenv
requests
# torch is installed separately in the Dockerfile with the CPU-only index URL
# so it is intentionally omitted here to avoid downloading the 2 GB CUDA build.
yfinance
pandas
numpy<2                 # many quant libs (arch, statsmodels) not yet numpy-2 stable
transformers
apscheduler
quantstats
scipy
statsmodels
feedparser
arch
xgboost
